"""
Persistence tests use the database fixtures defined in tests/conftest.py.

The fixtures are intentionally defined at the tests/ level because
integration tests outside this directory also require the same isolated
test PostgreSQL database.
"""
